const auth = require('./auth');
const cabinet = require('./cabinet');
const upload = require('./upload');
const cabinetAdmin = require('./cabinetAdmin');
const allRecipes = require('./allRecipes');

module.exports = {
  auth,
  cabinet,
  upload,
  cabinetAdmin,
  allRecipes
};
